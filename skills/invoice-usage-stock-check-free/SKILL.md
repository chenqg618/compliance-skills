---
slug: invoice-usage-stock-check-free
displayName: 发票领用存与开票数据核对（免费版）
version: 1.0.52
summary: 发票领用存台账逐项核对，每条结论都带原文依据，不需要付款，也不需要注册。
tags: [发票领用存与开票数据核对,发票领用存台账,核对,对账,免费]
license: Proprietary
name: invoice-usage-stock-check-free
display_name: 发票领用存与开票数据核对（免费）
display_name_en: Invoice Usage and Stock Check (Free)
description: 发票领用存台账逐项核对（逐行复算、合计勾稽、重复与空缺检测），每条结论引用原文。本免费版执行引擎声明的免费检查项。触发词包括 发票领用存与开票数据核对、发票领用存台账对不上。
description_zh: 发票领用存台账逐项核对（逐行复算、合计勾稽、重复与空缺检测），每条结论引用原文。本免费版执行引擎声明的免费检查项。触发词包括 发票领用存与开票数据核对、发票领用存台账对不上。
description_en: A free deterministic check for 发票领用存台账. Every finding cites the source. No payment, no registration, no API key, no network.
category: business-ops
author: WorkBuddy 开放平台开发者
allowed-tools: Read, Bash
---

# 发票领用存与开票数据核对（免费）

> **完全本地运行**：引擎已经打包在本技能里（`engine/` 目录）。
> 核对过程不需要联网、不需要 API Key、**没有调用次数上限**。

## 什么时候用

**谁会在什么时候用**：财务、出纳等岗位在**每月结账 / 对账 / 盘点**这类固定节点上，
必须把这张表核一遍才能往下走。

**这张表会逐项核对什么**：发票领用存台账的逐行算术、合计勾稽、重复与空缺检测。

每条结论都带**原文行号与出处**，可被第三方用同一口径复算；
材料不足时**不给结论**，会明确列出还缺什么。

## 这个免费版查什么

（检查项由引擎的 `CHECKS_*` 导出，跑 `--sample` 会打印实际执行了哪些。）

## 怎么用

```bash
node scripts/run.mjs --sample                 # 先看样例
node scripts/run.mjs --input 你的材料.json     # 跑自己的材料
```

## 常见错法（这张表里真的会有人这么填）

1. **期初库存填"上月结存"却不与上月期末对齐**：跨月衔接是完整档会查的项；手工抄数最容易在这里错一位。
2. **把金额填进份数列**：份数必须是整数，出现小数说明台账把开票金额和发票份数混在一列了。
3. **作废与红冲都记在"作废"一列**：作废（当月作废重开）和红冲（跨月冲销）是两件事，混在一起作废率会虚高。
4. **只填开出，不填领用**：没有领用记录就开出，只能吃期初库存 —— 期初一旦填错，整张表都跟着错。
5. **把电子发票和纸质发票混在一张台账里**：两者领用规则不同，建议分开两张表分别核对。

## 需要完整档时（可选）

本免费版是**完整可用的核心产出**，本版本**不包含**下面这些项：

- 跨月衔接检测（本月期初 = 上月期末）
- 作废率异常检测（作废 ÷ 开出 > 5%）
- 红冲率异常检测（红冲 ÷ 开出 > 3%）
- 份数非整数检测（发票份数必须是整数）
- 无领用却开出检测（本月未领用但开出了发票）

同名工具的**完整档**是另一个独立商品（条款与条件以它自己的商品页为准）；两档的差别**就是上面这些，没有别的**。

## 使用次数与完整档

本免费版每次运行结束后会向服务端做**一次匿名次数统计**：只发送本包标识和一个不含任何材料的匿名安装标识（由主机名与用户名做短哈希得到），**不发送你的材料、不写任何文件**。

· 前 3 次：服务端不返回任何内容，输出与现在完全一样；
· 第 4 次起：服务端会返回一段**完整档说明**（完整档是另一个独立商品，条款与价格以它自己的商品页为准），届时才会打印这一行；
· 统计不可用（离线/超时/被拦截）时什么都不做，**核对结果与退出码不受任何影响**。

本版本不包含的检查项见上一节；需要完整档时按服务端返回的官方口令执行，付款由用户本人确认。

## 边界（请务必知道）

本工具**不做**引擎 `CHECKS_OUT_OF_SCOPE` 里列的那些判断；
材料不足时**不给结论**，也不会输出「未发现问题」。
