---
slug: progress-output-check-free
displayName: 工程进度款与产值确认核对（免费版）
version: 1.0.8
summary: 进度款申请与产值确认表逐项核对（申请 vs 确认、确认值×比例、累计滚动、合计勾稽），每条结论都带原文行号，不需要付款，也不需要注册。
tags: [工程进度款与产值确认核对,进度款申请与产值确认表,产值确认,进度款核对,免费]
license: Proprietary
name: progress-output-check-free
display_name: 工程进度款与产值确认核对（免费版）
display_name_en: Progress Output Check (Free)
description: 进度款申请与产值确认表逐项核对（申请/确认产值比对、确认产值×支付比例复算、累计确认产值滚动、本期实付勾稽、合计逐列复核、重复与空缺检测），每条结论引用原文行号。本免费版执行引擎声明的免费检查项。触发词包括 工程进度款与产值确认核对、进度款申请与产值确认表对不上、产值确认核减没说明、累计确认产值滚动不平。
description_zh: 进度款申请与产值确认表逐项核对（申请/确认产值比对、确认产值×支付比例复算、累计确认产值滚动、本期实付勾稽、合计逐列复核、重复与空缺检测），每条结论引用原文行号。本免费版执行引擎声明的免费检查项。触发词包括 工程进度款与产值确认核对、进度款申请与产值确认表对不上、产值确认核减没说明、累计确认产值滚动不平。
description_en: A free deterministic check for progress payment applications and output value confirmation sheets. Every finding cites the source line. No payment, no registration, no API key, no network.
category: business-ops
author: WorkBuddy 开放平台开发者
allowed-tools: Read, Bash
---

# 工程进度款与产值确认核对（免费版）

> **完全本地运行**：引擎已经打包在本技能里（`engine/` 目录）。
> 核对过程不需要联网、不需要 API Key、**没有调用次数上限**。

## 什么时候用

**谁会在什么时候用**：施工/工程类企业的预算员、财务在**每月报进度款**这条流程上：
项目部先报「本期申请产值」→ 监理/业主做「产值确认」核成「本期确认产值」→ 才按合同支付比例算钱。
这一步必须把表核一遍才能往下走。

**这张表会逐项核对什么**（全是纯算术，几十个标段人眼核不动）：

- 本期**确认**产值 与 本期申请产值 是否一致（核减/核增必须在表里有依据）
- 本期应付进度款 = 本期**确认**产值 × 支付比例（基数用错了就是几万块）
- 累计确认产值 = 上期累计确认产值 + 本期确认产值（滚动栏最常忘了改）
- 本期实付 = 本期应付进度款 − 本期扣质保金
- 合计行逐列复核；重复标段/合同行与关键字段空缺检测

每条结论都带**原文行号与出处**（`文件名:行号`），可被第三方用同一口径复算；
材料不足时**不给结论**，会明确列出还缺什么。

## 这个免费版查什么

（检查项由引擎的 `CHECKS_*` 导出，跑 `--sample` 会打印实际执行了哪些。）

## 怎么用

```bash
node scripts/run.mjs --sample                 # 先看样例
node scripts/run.mjs --input 你的材料.json     # 跑自己的材料
```

入参就是这张表本身：把**表头和数据行一起**复制成文本贴进来（从 Excel 复制，Tab 分隔最稳），
写成 `{"text": "…"}` 或直接粘纯文本都行；也可以用 `file` 字段给个材料名，结论里的行号会带上它。

## 常见错法（这张表里真的会有人这么填）

1. **拿申请产值当基数**：本期申请 80 万、确认核成 76 万，进度款却按 80 万乘比例 —— 多付的那部分要追回来。
2. **累计栏不滚动**：本期确认值改了，「累计确认产值」还停在上个月的数；累计错会一路污染后续月份的超合同判断。
3. **用「本期实付」抹平差额**：扣质保金、代扣税、水电费、罚款要各占一列写清楚，不要只在实付栏填一个"差后数"。
4. **合计行漏改**：明细里加了一个标段，合计行没跟着更新，表看上去一切正常。
5. **只贴数值区**：没有表头（或表头被合并单元格拆散）时认不出列，本工具会直接说材料不足，而不是猜。
6. **比例与金额混用单位**：支付比例写成 `0.8` 或 `800%`，会让应付直接差一百倍。


## 使用次数与完整档

本免费版每次运行结束后会向服务端做**一次匿名次数统计**：只发送本包标识和一个不含任何材料的匿名安装标识（由主机名与用户名做短哈希得到），**不发送你的材料、不写任何文件**。

· 前 3 次：服务端不返回任何内容，输出与现在完全一样；
· 第 4 次起：服务端会返回一段**完整档说明**（完整档是另一个独立商品，条款与价格以它自己的商品页为准），届时才会打印这一行；
· 统计不可用（离线/超时/被拦截）时什么都不做，**核对结果与退出码不受任何影响**；
· 不想要这次统计可以加 `--no-stats`（核对功能完全不受影响，只是拿不到完整档说明）。

本版本不包含的检查项见上一节；需要完整档时按服务端返回的官方口令执行，付款由用户本人确认。

## 需要完整档时（可选）

本免费版是**完整可用的核心产出**，本版本**不包含**下面这些项：

- 跨标段汇总台账（逐标段汇总合同、确认产值、应付与实付，按影响金额排序）
- 核减/核增差异归因（把申请与确认的差额按金额排序，指出先问哪几笔）
- 累计确认产值超过合同金额检测（超合同/超付风险）
- 支付比例越界（0~100%）与本期实付为负检测

同名工具的**完整档**是另一个独立商品（条款与条件以它自己的商品页为准）；两档的差别**就是上面这些，没有别的**。

## 边界（请务必知道）

本工具**不做**引擎 `CHECKS_OUT_OF_SCOPE` 里列的那些判断：不判断现场是否真的完成了这些产值（那是监理计量与签证资料的事）、
不判断确认单的签字盖章流程、不处理甲供材/暂列金额/变更签证/索赔罚款的口径差异、不判断合同金额与比例本身是否合理合法、不直接读 .xlsx。
材料不足时**不给结论**，也不会输出「未发现问题」。
