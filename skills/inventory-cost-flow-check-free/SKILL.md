---
slug: inventory-cost-flow-check-free
displayName: 存货出入库与加权平均成本核对（免费）
version: 1.0.2
summary: 存货出入库与成本表逐项核对，每条结论都带原文依据，不需要付款，也不需要注册。
tags: [存货出入库与加权平均成本核对,存货出入库与成本表,核对,对账,免费]
license: Proprietary
name: inventory-cost-flow-check-free
display_name: 存货出入库与加权平均成本核对（免费）
display_name_en: Inventory Cost Flow Check (Free)
description: 存货出入库与成本表逐项核对（逐行复算、合计勾稽、重复与空缺检测），每条结论引用原文。本免费版执行引擎声明的免费检查项。触发词包括 存货出入库与加权平均成本核对、存货出入库与成本表对不上。
description_zh: 存货出入库与成本表逐项核对（逐行复算、合计勾稽、重复与空缺检测），每条结论引用原文。本免费版执行引擎声明的免费检查项。触发词包括 存货出入库与加权平均成本核对、存货出入库与成本表对不上。
description_en: A free deterministic check for 存货出入库与成本表. Every finding cites the source. No payment, no registration, no API key, no network.
category: business-ops
author: WorkBuddy 开放平台开发者
allowed-tools: Read, Bash
---

# 存货出入库与加权平均成本核对（免费）

> **完全本地运行**：引擎已经打包在本技能里（`engine/` 目录）。
> 不联网、不需要 API Key、**没有调用次数上限**。

## 什么时候用

**谁会在什么时候用**：财务、出纳等岗位在**每月结账 / 对账 / 盘点**这类固定节点上，
必须把这张表核一遍才能往下走。

**这张表会逐项核对什么**：存货出入库与成本表的逐行算术、合计勾稽、重复与空缺检测。

每条结论都带**原文行号与出处**，可被第三方用同一口径复算；
材料不足时**不给结论**，会明确列出还缺什么。

## 这个免费版查什么

（检查项由引擎的 `CHECKS_*` 导出，跑 `--sample` 会打印实际执行了哪些。）

## 怎么用

```bash
node scripts/run.mjs --sample                 # 先看样例
node scripts/run.mjs --input 你的材料.json     # 跑自己的材料
```

## 常见错法（这张表里真的会有人这么填）

1. **只填数量不填金额**：本工具核的是**数量与金额两条线**；只给一条线，勾稽就只剩一半。
2. **出库金额按上月单价结转**：那是移动加权或上月价，不是本月末一次加权 —— 会直接报"出库成本与加权平均不符"。
3. **期初数量有、期初金额空**：单价为零会把出库成本整体拉低，这是成本失真最常见的起点。
4. **退货用负数混进出库列**：建议单独列"退货/红冲"，否则负数量会让加权平均单价失去意义。
5. **同一存货拆成多行（不同仓库/批次）却不合并**：会报"同一存货出现多行"，应先把同一存货合并再核加权平均。

## 需要完整档时（可选）

本免费版是**完整可用的核心产出**，本版本**不包含**下面这些项：

- 出库单价偏离加权平均单价检测（偏离率 > 1%）
- 期末数量为负检测
- 出库数量超过可用数量检测（期初 + 入库）
- 数量或金额为负检测
- 期初有数量无金额（单价为零）检测

需要它们的话，同名工具的**完整档**里都有——完整档是**另一个独立商品**（在 SkillPay 货架上，条款与条件以它自己的商品页为准）。两档的差别**就是上面这些，没有别的**。

- 货架：**AI 核对工具铺** <https://skillpay.alipay.com/public/tokendidi>
- 找它：按商品名 **「存货出入库与加权平均成本核对 · 完整版」**

## 边界（请务必知道）

本工具**不做**引擎 `CHECKS_OUT_OF_SCOPE` 里列的那些判断；
材料不足时**不给结论**，也不会输出「未发现问题」。
