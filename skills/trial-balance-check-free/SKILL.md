---
slug: trial-balance-check-free
displayName: 试算平衡与期末结转核对（免费版）
version: 1.0.49
summary: 科目余额表逐项核对（免费版）。资产负债类逐行复算期末余额、损益类期末结转为零、借方合计与贷方合计相等、明细账与总账勾稽、科目层级小计、重复科目与空缺非数字，每条结论都带原文行号。
tags: [试算平衡,期末结转,结账核对,科目余额表,对账,免费]
license: Proprietary
name: trial-balance-check-free
display_name: 试算平衡与期末结转核对（免费版）
display_name_en: Trial Balance Check (Free)
description: 公司每月结账、每年审计前必做的科目余额表核对（免费版）。逐行复算资产负债类「期末余额 = 期初余额 + 本期借方发生额 − 本期贷方发生额」、检查损益类期末是否结转为零、期初与本期与期末三处借贷合计是否相等、明细账与总账勾稽、科目层级小计、重复科目、空缺与非数字。每条结论都带原文行号，可由第三方用同一份输入复算，不需要付款，也不需要注册。
description_zh: 公司每月结账、每年审计前必做的科目余额表核对（免费版）。逐行复算资产负债类「期末余额 = 期初余额 + 本期借方发生额 − 本期贷方发生额」、检查损益类期末是否结转为零、期初与本期与期末三处借贷合计是否相等、明细账与总账勾稽、科目层级小计、重复科目、空缺与非数字。每条结论都带原文行号，可由第三方用同一份输入复算，不需要付款，也不需要注册。
description_en: A free deterministic checker for a Chinese-style trial balance sheet (account balance table). It re-computes closing balances row by row, checks that profit and loss accounts are closed to zero, that debit and credit totals agree, that parent account subtotals match their children, and flags duplicate or blank cells. Every finding cites the source line. Runs locally, no network, no API key.
category: business-ops
author: WorkBuddy 开放平台开发者
allowed-tools: Read, Bash
---

# 试算平衡与期末结转核对（免费版）

> **完全本地运行**：引擎已经打包在本技能里（`engine/` 目录）。
> 核对过程不需要联网、不需要 API Key、**没有调用次数上限**。

## 什么时候用

**谁会在什么时候用**：会计与财务主管在**每月结账时**、财务经理在**每年审计进场前**，
都要先把这张科目余额表（试算平衡表）核一遍 —— 账平了才能出报表、才能交给审计。
一笔借贷不平会带着错一路进到资产负债表和利润表，所以这是"每月必须做、且完全算得出来对错"的事。

**这张表会逐项核对什么**：

- 资产负债类逐行复算：期末余额 = 期初余额 + 本期借方发生额 − 本期贷方发生额
- 损益类期末结转为零：结转后期末余额必须为 0，且发生额列应是含结转分录的结转后口径
- 借贷合计相等：期初余额合计、本期借方发生额合计与本期贷方发生额合计、期末余额合计
- 明细账与总账勾稽：表里的合计行必须等于各一级科目行之和
- 科目层级小计：上级科目 = 其直接下级科目之和（逐列复核）
- 重复科目：同一科目编码出现多行
- 空缺与非法值：必填列为空、占位符、非数字、科目类别无法识别

每条结论都带**原文行号与出处**，可被第三方用同一份输入复算；
材料不足时**不给结论**，会明确列出还缺什么。

**与已有能力的区别**：本工具核的是**账套自己的试算平衡**（科目余额表内部勾稽与期末结转）；
发票本身的一致性、增值税申报口径的进销项核算，都是另外的能力，不在本工具范围内。

## 材料怎么给

把科目余额表**连同表头**从 Excel 复制成文本贴进来（Tab 分隔最稳），列头要能认出
「科目编码 / 科目名称 / 科目类别 / 期初余额 / 本期借方发生额 / 本期贷方发生额 / 期末余额」。

**金额口径（务必一致）**：借贷方向带符号 —— **借方余额为正、贷方余额为负**；
本期借方、本期贷方两列都填正数；发生额列用**结转后口径**（含损益结转分录）。

## 怎么用

```bash
node scripts/run.mjs --sample                 # 先看内置样例（一张干净的表）
node scripts/run.mjs --input 你的材料.json     # 跑自己的材料
node scripts/run.mjs --input 你的材料.json --json
```

## 常见错法（这张表里真的会有人这么填）

1. **负债/权益余额填成正数**：应付账款填 `150000.00` 而不是 `-150000.00`，
   于是「期初 + 借 − 贷 = 期末」永远差两倍，期初合计也不可能为 0。贷方余额必须填负数。
2. **发生额列只填业务发生额、不含期末结转分录**：损益类科目本期借方 ≠ 贷方，
   借贷合计当场不平 —— 请用"结转后"口径导出，借、贷两列都含结转分录。
3. **一级科目行与明细行一起塞进表里又都计入合计**：合计行把 `1122 应收账款` 和
   `1122.01 / 1122.02` 加了两次，金额直接翻倍。要么只列一级科目，要么让上级行只当小计。
4. **损益类余额留在表里不结转**：管理费用期末还挂着 90000，说明期末结转分录没做；
   损益类是"期末必然为 0"的科目，留在表里本身就是错。
5. **只核"借方合计 = 贷方合计"就以为平了**：期初余额合计、期末余额合计同样必须借贷相等，
   只盯本期发生额会漏掉上期带下来的错。
6. **用括号或带单位写负数**：`(1,000.00)`、`1,000.00元` 都会解析不成数字；
   请写成 `-1000.00` 这样的规范数字。


## 需要完整档时（可选）

本免费版是**完整可用的核心产出**，本版本**不包含**下面这些项：

- 结账前不平差额归因（漏记 / 重复记账 / 科目串户 / 结转未做 / 方向反了，逐条给出证据行号）
- 无法用行级证据归因的差额如实标为「待人工确认」并单列残余金额（不硬套原因）
- 结账前处理清单（按金额排序的调整分录，写明借/贷科目与金额，每条都带原文行号）

同名工具的**完整档**是另一个独立商品（条款与条件以它自己的商品页为准）；两档的差别**就是上面这些，没有别的**。

## 使用次数与完整档

本免费版每次运行结束后会向服务端做**一次匿名次数统计**：只发送本包标识和一个不含任何材料的匿名安装标识（由主机名与用户名做短哈希得到），**不发送你的材料、不写任何文件**。

· 前 3 次：服务端不返回任何内容，输出与现在完全一样；
· 第 4 次起：服务端会返回一段**完整档说明**（完整档是另一个独立商品，条款与价格以它自己的商品页为准），届时才会打印这一行；
· 统计不可用（离线/超时/被拦截）时什么都不做，**核对结果与退出码不受任何影响**。

本版本不包含的检查项见上一节；需要完整档时按服务端返回的官方口令执行，付款由用户本人确认。

## 边界（请务必知道）

本工具**不做**引擎 `CHECKS_OUT_OF_SCOPE` 里列的那些判断（科目使用是否正确、跨年调整、
未达账项与暂估、读取 Excel 账套）；材料不足时**不给结论**，也不会输出「未发现问题」。
