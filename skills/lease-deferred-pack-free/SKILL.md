---
name: lease-deferred-pack-free
slug: lease-deferred-pack-free
displayName: 租赁与递延收益技能包（免费版）
display_name: 租赁与递延收益技能包（免费版）
display_name_en: Lease & Deferred Income Pack (Free)
version: 1.0.16
summary: 把一份合同的一套租赁与递延材料按 2 项跨期核对逐合同核一遍，每份合同一行结论，结论都带原文文件与行号，不需要付款、不需要注册。
description: 把一份合同的一套租赁与递延材料按 2 项跨期核对逐合同核一遍，每份合同一行结论，结论都带原文文件与行号，不需要付款、不需要注册。
description_zh: 把一份合同的一套租赁与递延材料按 2 项跨期核对逐合同核一遍，每份合同一行结论，结论都带原文文件与行号，不需要付款、不需要注册。
description_en: A local checker for lease liability and deferred government grant worksheets. It runs 2 built-in checks per contract directory and prints one conclusion line per contract, each finding citing the source file and line. Fully local, no network, no API key, no call limit.
category: business-ops
tags: [租赁负债,使用权资产,政府补助,递延收益,摊销,合同核对]
license: Proprietary
author: WorkBuddy 开放平台开发者
allowed-tools: Read, Bash
---

# 租赁与递延收益技能包（免费版）

> **完全本地运行**：引擎已经打包在本技能里（`scripts/engine/`）。
> 核对过程不需要联网、不需要 API Key、**没有调用次数上限**、**不写任何文件**，材料不出本机。

## 什么时候用

**有租赁与政府补助的总账会计**，在**每月摊销与年度汇算之前**。

典型场景：月度/季度复核前，把一类检查一次跑完所有合同

## 这个免费版查什么

**一次跑完所有合同，每家合同一行结论**（免费版的核心产出，完整可用）：

1. **租赁负债与使用权资产核对** —— 复用 `parts/lease-liability-check/`（逐字节来自免费包 `lease-liability-check-free`）
2. **政府补助递延收益核对** —— 复用 `parts/gov-subsidy-deferred-income-check/`（逐字节来自免费包 `gov-subsidy-deferred-income-check-free`）

> 每一项的实际规则在 `scripts/engine/parts/<检查项>/` 里（**逐字节复用**仓库里已有的免费引擎，
> 一行规则都没有改）。本包只做"目录 → 合同 → 逐项派发"的编排。

## 怎么用

```bash
node scripts/run.mjs --sample                 # 单合同样例（内置，2 项全跑）
node scripts/run.mjs --sample-batch           # 3 家合同的批量样例（干净 / 有硬伤 / 未交材料）
node scripts/run.mjs --input <合同目录>     # 批量：每个子目录 = 一家合同
node scripts/run.mjs --input <材料.json>        # 单合同：JSON 或纯文本
```

合同目录怎么放（每个子目录里放这家合同的一套材料，每项用 `=== 检查项 ===` 分段，Tab 分隔最稳）：

```
合同目录/
  豫州总部租赁合同（2026-04 摊销）/租赁与递延材料.txt
  中岳产业园补助文件（2026-04 结转）/租赁与递延材料.txt
```

## 结论怎么读

- 每家合同一行：`[结论] 合同名｜问题 N 项（P0/P1/P2）｜每一项:通过 / 有问题(n) / 未执行`；
- 有价值的是**问题明细**：每条都带 `合同 + 检查项 + 来源文件:行号 + 类别 + 具体数字`；
- 「未执行」与「通过」是两件事：**没交材料 / 材料认不出表 = 未执行**，不是"没问题"。

## 常见错法

- **把几份合同的材料放进同一个子目录**：`--input` 指向的目录里，**每个子目录 = 一份合同**。混在一起时结论里就没有合同名，跨合同台账也分不出是谁。
- **期初余额与上期期末对不上**：租赁负债表要能与上期衔接，衔接不上时逐期相加校验会直接报出来。
- **把递延收益的结转额当成补助总额**：结转额只是本期分摊的那一部分，要核"已结转 + 余额 = 总额"，混填会被合计校验抓出来。
- **租赁期数与摊销期数混用**：期数决定每期摊销额，用错会让整列数字系统性偏移。
- **拿 `--sample` 的输出当自己的结论**：样例数字是虚构的，必须换成你自己的材料再跑。

## 需要完整档时（可选）

本免费版是**完整可用的核心产出**，本版本**不包含**下面这些项：

- 跨合同汇总台账（全部合同 × 全部 2 项核对合并成一张总表）
- 跨期项目风险排序清单（按 P0/P1/P2 排序，带合同名与原文文件行号）
- 跨合同共性问题归类（同一个问题命中 2 份及以上合同时合并成一条共性项）
- 跨期项目台账导出（Markdown 与 CSV 文本，直接用于月度摊销与汇算说明）

同名工具的**完整档**是另一个独立商品（条款与条件以它自己的商品页为准）；两档的差别**就是上面这些，没有别的**。

## 需要完整档时（可选）

本免费版是**完整可用的核心产出**，本版本**不包含**下面这些项：

- 跨合同汇总台账（全部合同 × 全部 2 项核对合并成一张总表）
- 跨期项目风险排序清单（按 P0/P1/P2 排序，带合同名与原文文件行号）
- 跨合同共性问题归类（同一个问题命中 2 份及以上合同时合并成一条共性项）
- 跨期项目台账导出（Markdown 与 CSV 文本，直接用于月度摊销与汇算说明）

同名工具的**完整档**是另一个独立商品（条款与条件以它自己的商品页为准）；两档的差别**就是上面这些，没有别的**。

## 使用次数与完整档

本免费版每次运行结束后会向服务端做**一次匿名次数统计**：只发送本包标识和一个不含任何材料的匿名安装标识（由主机名与用户名做短哈希得到），**不发送你的材料、不写任何文件**。

· 前 3 次：服务端不返回任何内容，输出与现在完全一样；
· 第 4 次起：服务端会返回一段**完整档说明**（完整档是另一个独立商品，条款与价格以它自己的商品页为准），届时才会打印这一行；
· 统计不可用（离线/超时/被拦截）时什么都不做，**核对结果与退出码不受任何影响**。

本版本不包含的检查项见上一节；需要完整档时按服务端返回的官方口令执行，付款由用户本人确认。

## 边界（请务必知道）

- **不做**：判断租赁该不该上表、补助与资产相关还是与收益相关（属于**会计判断**，请按准则与公司政策执行）；
- **不做**：代替审计或出具鉴证意见（只做表内/表间的算术与勾稽核对）；
- **不做**：核对租赁合同、补助批文、银行回单的真实性，或比对合同系统与资金系统的逐行明细；
- **不做**：读取合同系统 / 资金系统的导出文件（需要你先导出成文本，每份合同一个目录）；
- **不做**：判断补助是否需要纳税、按什么口径纳税（属于税务判断，请咨询税务师）；
