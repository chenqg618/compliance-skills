---
slug: budget-variance-check-free
displayName: 费用预算执行差异核对（免费版）
version: 1.0.58
summary: 预算执行表逐项核对，每条结论都带原文依据，不需要付款，也不需要注册。
tags: [费用预算执行差异核对,预算执行表,核对,对账,免费]
license: Proprietary
name: budget-variance-check-free
display_name: 费用预算执行差异核对（免费）
display_name_en: Budget Variance Check (Free)
description: 预算执行表逐项核对（逐行复算、合计勾稽、重复与空缺检测），每条结论引用原文。本免费版执行引擎声明的免费检查项。触发词包括 费用预算执行差异核对、预算执行表对不上。
description_zh: 预算执行表逐项核对（逐行复算、合计勾稽、重复与空缺检测），每条结论引用原文。本免费版执行引擎声明的免费检查项。触发词包括 费用预算执行差异核对、预算执行表对不上。
description_en: A free deterministic check for 预算执行表. Every finding cites the source. No payment, no registration, no API key, no network.
category: business-ops
author: WorkBuddy 开放平台开发者
allowed-tools: Read, Bash
---

# 费用预算执行差异核对（免费）

> **完全本地运行**：引擎已经打包在本技能里（`engine/` 目录）。
> 核对过程不需要联网、不需要 API Key、**没有调用次数上限**。

## 什么时候用

**谁会在什么时候用**：财务、出纳等岗位在**每月结账 / 对账 / 盘点**这类固定节点上，
必须把这张表核一遍才能往下走。

**这张表会逐项核对什么**：预算执行表的逐行算术、合计勾稽、重复与空缺检测。

每条结论都带**原文行号与出处**，可被第三方用同一口径复算；
材料不足时**不给结论**，会明确列出还缺什么。

## 这个免费版查什么

（检查项由引擎的 `CHECKS_*` 导出，跑 `--sample` 会打印实际执行了哪些。）

## 怎么用

```bash
node scripts/run.mjs --sample                 # 先看样例
node scripts/run.mjs --input 你的材料.json     # 跑自己的材料
```

## 常见错法（这张表里真的会有人这么填）

1. **差异率与差异方向填反**：超支应是正差异率、结余是负；符号反了管理层会看反结论（完整档会报"方向矛盾"）。
2. **预算数为 0 却填差异率**：除数为零算不出比率；这类科目应当留空或注明"新增科目"。
3. **累计实际直接手抄**：完整档会核"累计 = 逐期实际之和"，跨月抄错是最常见的对不上。
4. **把年度预算填进"预算数"**：月度执行表应当是**当月预算**；填年度数会让执行率看起来只有几个百分点。
5. **同一科目拆成多行（不同部门）却不注明**：会报"同一科目出现多行"，建议在科目后加部门。


## 使用次数与完整档

本免费版每次运行结束后会向服务端做**一次匿名次数统计**：只发送本包标识和一个不含任何材料的匿名安装标识（由主机名与用户名做短哈希得到），**不发送你的材料、不写任何文件**。

· 前 3 次：服务端不返回任何内容，输出与现在完全一样；
· 第 4 次起：服务端会返回一段**完整档说明**（完整档是另一个独立商品，条款与价格以它自己的商品页为准），届时才会打印这一行；
· 统计不可用（离线/超时/被拦截）时什么都不做，**核对结果与退出码不受任何影响**。

本版本不包含的检查项见上一节；需要完整档时按服务端返回的官方口令执行，付款由用户本人确认。

## 需要完整档时（可选）

本免费版是**完整可用的核心产出**，本版本**不包含**下面这些项：

- 执行率勾稽（实际数 ÷ 预算数 = 执行率）
- 累计实际与逐月/逐项实际不符检测
- 预算数或实际数为负检测
- 执行率超出常见区间（0~200%）检测
- 差异率与差异方向不一致检测（正负号矛盾）

同名工具的**完整档**是另一个独立商品（条款与条件以它自己的商品页为准）；两档的差别**就是上面这些，没有别的**。

## 边界（请务必知道）

本工具**不做**引擎 `CHECKS_OUT_OF_SCOPE` 里列的那些判断；
材料不足时**不给结论**，也不会输出「未发现问题」。
