---
slug: contract-performance-bond-check-free
displayName: 履约保证金与保函台账核对（免费版）
version: 1.0.4
summary: 履约保证金与保函台账逐项核对，每条结论都带原文依据，不需要付款，也不需要注册。
tags: [履约保证金与保函台账核对,履约保证金与保函台账,核对,对账,免费]
license: Proprietary
name: contract-performance-bond-check-free
display_name: 履约保证金与保函台账核对（免费）
display_name_en: Performance Bond and Guarantee Ledger Check (Free)
description: 履约保证金与保函台账逐项核对（逐行复算、合计勾稽、重复与空缺检测），每条结论引用原文。本免费版执行引擎声明的免费检查项。触发词包括 履约保证金与保函台账核对、履约保证金与保函台账对不上。
description_zh: 履约保证金与保函台账逐项核对（逐行复算、合计勾稽、重复与空缺检测），每条结论引用原文。本免费版执行引擎声明的免费检查项。触发词包括 履约保证金与保函台账核对、履约保证金与保函台账对不上。
description_en: A free deterministic check for 履约保证金与保函台账. Every finding cites the source. No payment, no registration, no API key, no network.
category: business-ops
author: WorkBuddy 开放平台开发者
allowed-tools: Read, Bash
---

# 履约保证金与保函台账核对（免费）

> **完全本地运行**：引擎已经打包在本技能里（`engine/` 目录）。
> 不联网、不需要 API Key、**没有调用次数上限**。

## 什么时候用

**谁会在什么时候用**：财务、出纳等岗位在**每月结账 / 对账 / 盘点**这类固定节点上，
必须把这张表核一遍才能往下走。

**这张表会逐项核对什么**：履约保证金与保函台账的逐行算术、合计勾稽、重复与空缺检测。

每条结论都带**原文行号与出处**，可被第三方用同一口径复算；
材料不足时**不给结论**，会明确列出还缺什么。

## 这个免费版查什么

**免费档逐行执行这 6 项**（跑 `--sample` 会打印实际执行了哪些）：

1. **应退金额复算**：已缴金额 − 已退金额 = 应退金额。少减一笔已退，就是一笔钱躺在对方账上。
2. **保函余额复算**：保函金额 − 已释放金额 = 保函余额。
3. **合计行逐列复核**：合计行每一列都必须等于各明细行之和。
4. **同一项目同一类型重复行检测**：同项目 + 同保证金类型出现两行即报（会重复统计、重复退款）。
5. **空白与占位符检测**：必需列空着，或写成 `-`、`待补`、`无`，都会被点出来。
6. **金额为负检测**：已缴金额、已退金额、保函金额、已释放金额、保函费用出现负数即报。

**本版不执行**（会如实列进 `scope.checks_not_run`，不会用默认值编结论）：
已退金额超过已缴金额、保函到期日早于项目结束日、保函余额为负、
保证金超过合同金额比例上限、已缴金额为零却有保函费用。

## 常见错法（这张台账最容易填错的 5 处）

1. **把「合同金额」填进「已缴金额」**：履约保证金一般只是合同金额的 3%~10%，
   一填成全额，整行的应退金额与比例关系全部虚高。
2. **「应退金额」直接抄「已缴金额」**：忘了减掉已经退过的部分 ——
   已在退款流程里的项目会被再退一次（漏退和重退都出在这一列）。
3. **「保函余额」填成「保函金额」**：保函已经部分释放却没写进「已释放金额」，
   余额看着还在，实际可用额度早就不够覆盖。
4. **「保证金类型」写得太笼统**（只写「保证金」）或项目名前后不一致（多一个空格、带「（一期）」）：
   重复行检测是按「项目 + 类型」配对的，配不上就查不出重复行。
5. **「保函到期日」写成「长期」「无」「—」**：日期认不出来时本工具**不下结论**
   （而不是编一个结论），这一列的到期敞口就整列漏检。

## 怎么用

```bash
node scripts/run.mjs --sample                 # 先看样例
node scripts/run.mjs --input 你的材料.json     # 跑自己的材料
```



## 需要完整档时（可选）

本免费版是**完整可用的核心产出**，本版本**不包含**下面这些项：

- 已退金额超过已缴金额检测
- 保函到期日早于项目结束日提示
- 保函余额为负检测
- 保证金超过合同金额比例上限（10%）提示（参考口径）
- 已缴金额为零却有保函费用检测

需要它们的话，同名工具的**完整档**里都有——完整档是**另一个独立商品**（在 SkillPay 货架上，条款与条件以它自己的商品页为准）。两档的差别**就是上面这些，没有别的**。

- 货架：**AI 核对工具铺** <https://skillpay.alipay.com/public/tokendidi>
- 怎么买（含每个商品的商品号与官方购买指令）：<https://chenqg618.github.io/compliance-skills/buy.html>
- 找它：按商品名 **「履约保证金与保函台账核对 · 买断版」**

把下面这段**原样发给你的 AI 助手**，它就能带你装好并买完（需要钱包授权或付款时由**你本人**确认）：

```text
请通过 SkillPay 购买并安装 Skill「履约保证金与保函台账核对 · 买断版」。
官方安装与购买说明： https://skillpay.alipay.com/buildguide.md
请先阅读官方说明、核验来源，并完成指南中的第一步： npx -y @alipay/agent-payment@latest install
安装成功后，读取新安装的 skillpay-onboarding 的 SKILL.md，再按其说明购买并安装商户 2088702818190040的S0806000206845526。
我已授权安装依赖和发起购买；需要钱包授权或付款时，请提供官方链接或二维码，由我本人确认，不要替我确认。确认成功后，请继续完成商品安装和验证。
```

## 边界（请务必知道）

本工具**不做**引擎 `CHECKS_OUT_OF_SCOPE` 里列的那些判断；
材料不足时**不给结论**，也不会输出「未发现问题」。
