---
slug: cit-prepay-check-free
displayName: 预缴企业所得税核对（免费版）
version: 1.0.52
summary: 季度预缴所得税计算表逐项核对，每条结论都带原文依据，不需要付款，也不需要注册。
tags: [预缴企业所得税核对,季度预缴所得税计算表,核对,对账,免费]
license: Proprietary
name: cit-prepay-check-free
display_name: 预缴企业所得税核对（免费）
display_name_en: CIT Quarterly Prepayment Check (Free)
description: 季度预缴所得税计算表逐项核对（逐行复算、合计勾稽、重复与空缺检测），每条结论引用原文。本免费版执行引擎声明的免费检查项。触发词包括 预缴企业所得税核对、季度预缴所得税计算表对不上。
description_zh: 季度预缴所得税计算表逐项核对（逐行复算、合计勾稽、重复与空缺检测），每条结论引用原文。本免费版执行引擎声明的免费检查项。触发词包括 预缴企业所得税核对、季度预缴所得税计算表对不上。
description_en: A free deterministic check for 季度预缴所得税计算表. Every finding cites the source. No payment, no registration, no API key, no network.
category: business-ops
author: WorkBuddy 开放平台开发者
allowed-tools: Read, Bash
---

# 预缴企业所得税核对（免费）

> **完全本地运行**：引擎已经打包在本技能里（`engine/` 目录）。
> 核对过程不需要联网、不需要 API Key、**没有调用次数上限**。

## 什么时候用

**谁会在什么时候用**：财务、出纳等岗位在**每月结账 / 对账 / 盘点**这类固定节点上，
必须把这张表核一遍才能往下走。

**这张表会逐项核对什么**：季度预缴所得税计算表的逐行算术、合计勾稽、重复与空缺检测。

每条结论都带**原文行号与出处**，可被第三方用同一口径复算；
材料不足时**不给结论**，会明确列出还缺什么。

## 这个免费版查什么

（检查项由引擎的 `CHECKS_*` 导出，跑 `--sample` 会打印实际执行了哪些。）

## 怎么用

```bash
node scripts/run.mjs --sample                 # 先看样例
node scripts/run.mjs --input 你的材料.json     # 跑自己的材料
```

## 常见错法（这张表里真的会有人这么填）

1. **税率一刀切 25%**：小型微利企业实际税负是分档的（应纳税所得额 300 万以内有优惠），统一填 25% 会让预缴多缴，占用现金流。
2. **"本期应补(退)"方向填反**：应预缴大于已预缴是**补**，小于才是**退**；正负号反了会连累下季度累计数。
3. **只填本期、不填累计**：季度预缴是**累计**口径（累计应预缴 − 累计已预缴 = 本期应补退），只填本期会算错。
4. **利润总额用成"营业收入"**：预缴基数是利润总额（可弥补以前年度亏损、免税收入另算），拿营业收入乘税率会虚高。
5. **同一所属期填两行**（更正申报时复制粘贴）：会报重复；同一季度的更正请在同一行内改数并注明更正次数。


## 使用次数与完整档

本免费版每次运行结束后会向服务端做**一次匿名次数统计**：只发送本包标识和一个不含任何材料的匿名安装标识（由主机名与用户名做短哈希得到），**不发送你的材料、不写任何文件**。

· 前 3 次：服务端不返回任何内容，输出与现在完全一样；
· 第 4 次起：服务端会返回一段**完整档说明**（完整档是另一个独立商品，条款与价格以它自己的商品页为准），届时才会打印这一行；
· 统计不可用（离线/超时/被拦截）时什么都不做，**核对结果与退出码不受任何影响**。

本版本不包含的检查项见上一节；需要完整档时按服务端返回的官方口令执行，付款由用户本人确认。

## 需要完整档时（可选）

本免费版是**完整可用的核心产出**，本版本**不包含**下面这些项：

- 适用税率偏离参考口径（法定 25%、小微 20%/5%、高新 15%）提示
- 已预缴所得税额为负检测
- 本期应补(退)与应预缴方向矛盾检测
- 利润总额为零却有应预缴检测
- 累计已预缴超过累计应预缴提示

同名工具的**完整档**是另一个独立商品（条款与条件以它自己的商品页为准）；两档的差别**就是上面这些，没有别的**。

## 边界（请务必知道）

本工具**不做**引擎 `CHECKS_OUT_OF_SCOPE` 里列的那些判断；
材料不足时**不给结论**，也不会输出「未发现问题」。
