---
slug: teacher-hour-pay-check-free
displayName: 课时消耗与教师课时费核对（免费版）
version: 1.0.1
summary: 课时消耗与教师课时费明细表逐项核对（课时费复算、正课/试听/补课口径、双线课耗勾稽、合计复核、保底单价、重复与空缺），每条结论都带原文行号，不需要付款，也不需要注册。
tags: [课时消耗与教师课时费核对,课时消耗与教师课时费明细表,课时费核对,学员消课,课耗勾稽,免费]
license: Proprietary
name: teacher-hour-pay-check-free
display_name: 课时消耗与教师课时费核对（免费版）
display_name_en: Teaching Hour Pay Check (Free)
description: 课时消耗与教师课时费明细表逐项核对（课时费=课时数×课时单价逐行复算、正课/试听/补课口径区分、学员消课课时与系统课耗课时逐笔勾稽、合计行逐列复核、课时单价低于合同保底单价、重复课时记录与关键字段空缺），每条结论引用原文行号。本免费版执行引擎声明的免费检查项。触发词包括 课时消耗与教师课时费核对、课时消耗与教师课时费明细表对不上、课时费算错了、学员消课和系统课耗对不上、课时单价低于保底。
description_zh: 课时消耗与教师课时费明细表逐项核对（课时费=课时数×课时单价逐行复算、正课/试听/补课口径区分、学员消课课时与系统课耗课时逐笔勾稽、合计行逐列复核、课时单价低于合同保底单价、重复课时记录与关键字段空缺），每条结论引用原文行号。本免费版执行引擎声明的免费检查项。触发词包括 课时消耗与教师课时费核对、课时消耗与教师课时费明细表对不上、课时费算错了、学员消课和系统课耗对不上、课时单价低于保底。
description_en: A free deterministic check for teaching-hour consumption and teacher hourly-pay sheets. Recomputes every pay line, separates regular/trial/make-up lesson rules, ties student hour consumption to the system ledger, re-adds every total column and flags missing or duplicated records. Every finding cites the source line. No payment, no registration, no API key, no network.
category: business-ops
author: WorkBuddy 开放平台开发者
allowed-tools: Read, Bash
---

# 课时消耗与教师课时费核对（免费版）

> **完全本地运行**：引擎已经打包在本技能里（`engine/` 目录）。
> 核对过程不需要联网、不需要 API Key、**没有调用次数上限**。

## 什么时候用

**谁会在什么时候用**：教培机构的教务 / 财务 / 校区主管，在**每月算教师课时费**这条流程上：
先把学员课时包的「课耗」导出来，再按排课记录算老师该拿多少课时费，两张数必须对到一起才能发钱。
月底一次要核好几张表（多个教师、多个校区），核完就得往下走。

**这张表会逐项核对什么**（全是纯算术，几十上百行没人愿意手核）：

- **课时费 = 课时数 × 课时单价**：逐行复算，单价录错一位、课时数漏乘都是钱
- **正课 / 试听 / 补课的口径不一样**：试听与补课**不消耗学员课时**（补课是服务补救，原课次已经扣过），
  正课必须有课耗 —— 把两种口径混着填，学员课时包和教师课时费两边都对不平
- **学员消课课时 = 系统课耗课时**：两条线的数必须逐笔相等，差一笔就有一边算错了
- **合计行逐列复核**：课时消耗合计、课时费合计、代课补贴合计必须等于明细之和
- **课时单价 ≥ 合同保底单价**：低于保底的排课直接亏
- **重复课时记录与关键字段空缺**：同一教师同一天同一课型同样课时数的重复行、缺格与占位符

每条结论都带**原文行号与出处**（`文件名:行号`），可被第三方用同一口径复算；
材料不足时**不给结论**，会明确列出还缺什么。

## 怎么用

```bash
node scripts/run.mjs --sample                 # 先看样例
node scripts/run.mjs --input 你的材料.json     # 跑自己的材料
```

入参就是这张表本身：把**表头和数据行一起**复制成文本贴进来（从 Excel 复制，Tab 分隔最稳），
写成 `{"text": "…"}` 或直接粘纯文本都行；也可以用 `file` 字段给个材料名，结论里的行号会带上它。
表头必须能认出这 11 列：教师、校区、课程类型、上课日期、课时数、课时单价、课时费、
代课补贴、学员消课课时、系统课耗课时、合同保底单价（少一列就会直接说"材料不足"，不会猜）。

## 出参

`run(payload)` 返回 `{status, result}`：

- `status`：`success`（已执行检查）或 `insufficient_input`（材料不足，**此时没有 `result`，也不给任何结论**）
- `result.findings[]`：每条 = `level`（P0/P1）、`category`、`line`（**原文行号**）、`amount`、
  `message`（带具体数字）、`evidence`（`文件名:行号`）、`advice`（怎么改）
- `result.summary`：条数、P0/P1 计数、`hours_total` / `pay_total` / `subsidy_total` /
  `consumed_total` / `sys_consumed_total`、`verdict`（`NO_ISSUE_FOUND` / `NEEDS_REVIEW` / `ERROR_FOUND`）
- `result.checks_executed` / `checks_withheld`：本次**真的执行了**哪些、哪些**没有执行**（如实列出，不伪造）

## 常见错法（本产品专属）

1. **拿「上课课时数」当学员课耗**：老师上了 2 课时，可学员请假后没扣课时 —— 两边用的根本不是同一个数。
2. **试听 / 补课也扣学员课时**：试听不该扣；补课是服务补救，原课次已经扣过，再扣就是重复扣费。
3. **课时费与「课时数 × 单价」对不上**：常见是调价后只改了单价没重算课时费，或者课时费那一格直接手填。
4. **只贴数值区不贴表头**：表头缺了或认不出来，本工具直接说材料不足，而不是猜哪一列是什么。
5. **合计行漏改**：明细里加了一行，合计行没跟着更新，表面看一切正常，课时消耗合计却对不上。
6. **同一节课录两遍**：同一教师同一天同一课型同样课时数出现两行，课时费会翻倍；本表没有学员列，
   要区分不同学员请自己加一列学员或课次编号。
7. **课时单价低于合同保底价**：低价排课（体验价、内部价）必须先有特批依据，否则就是在亏。

## 本版本不包含什么

本免费版是**完整可用的核心产出**；下面这些检查项**不在本版本里执行**（引擎会如实列在
`checks_withheld` 里，绝不会假装做过）：

- 跨教师 / 跨校区汇总台账（逐组汇总课时、课耗、实发课时费与差额，按差额从大到小排序）
- 按差额排序的处理清单（先核哪几笔，带原文行号与金额）
- 课时费差额归因：课时数差异 / 单价差异 / 代课补贴 / 录入口径残差
- 同一教师同一课程类型的课时单价一致性检测（同一课型不该出现两个价）
- 同一教师同一日课时数超过单日上限检测

需要这一层能力的是另一个独立商品，条款与条件以它自己的商品页为准；
两档的差别**就是上面这些，没有别的**。

## 使用次数与完整档

本免费版会在**本包目录**里记一个使用次数（`scripts/.hint-count`，一个整数，不含任何机器或用户信息）。

· 前 3 次：**完全离线**（不发生任何网络请求），输出与现在完全一样；
· 第 4 次起：服务端会返回一段**完整档说明**（完整档是另一个独立商品，条款与价格以它自己的商品页为准），届时才会打印这一行；
· 第 4 次起才会向服务端问一次（只发包标识与次数），拿回完整档说明；
· 请求不可用（离线/超时/被拦截）时什么都不做，**核对结果与退出码不受任何影响**；
· 不想要这次统计可以加 `--no-stats`（核对功能完全不受影响，只是拿不到完整档说明）。

本版本不包含的检查项见上一节；需要完整档时按服务端返回的官方口令执行，付款由用户本人确认。

## 边界（请务必知道）

本工具**不做**引擎 `CHECKS_OUT_OF_SCOPE` 里列的那些判断：
不判断排课、考勤与上课是否真实发生（那是教务排课系统与签到记录的事）、
不判断课时包售价、退费与赠课规则是否合规、不判断课时单价与代课补贴标准本身是否合理合法、
不处理跨月结转、跨校区调课、离职结算与个税社保代扣、不直接读 `.xlsx` / `.pdf`。
材料不足时**不给结论**，也不会输出「未发现问题」。
